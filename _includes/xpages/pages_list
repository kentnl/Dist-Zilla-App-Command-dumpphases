{% assign pages_list = site.xpages %}  
{% for node in pages_list %}
  {% if node.title != null %}
    <li class="active"><a href="{{node.url}}" class="active">{{node.title}}</a></li>
  {% endif %}
{% endfor %}
{% assign pages_list = nil %}
{% assign group = nil %}
